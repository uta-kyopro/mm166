外周タイルでは、**外周に出ている2辺がタイル内で直接接続される回転は、その2出口が指定ペアの場合に限って許可する**。
指定ペアでない外周出口同士を接続する回転は後から修正不能なので、**初期制約として候補から除外する**。

外周から内側へタイルの回転を決める Beam Search を試す。状態では部分経路の連結成分と未確定辺を管理し、指定外の出口ペアが接続した時点で枝刈りする。
外周2～4タイルの小パッチは回転を全列挙し、単項制約と隣接回転の二項制約を前計算する。
評価値は完成ペア数だけでなく、残り domain 上で全指定ペアが到達可能か、回転コスト下界、未確定成分数を用いる。

探索順は外周リングの固定順ではなく、外周制約を起点に domain を伝播し、未確定で domain が最小のタイルを選ぶMRVとする。
回転を仮定するたびに隣接制約を伝播し、domain消滅・誤ペア接続・必要出口の閉じ込め・楽観グラフ上の到達不能を検出して棄却する。
全ペアの接続可能性を第一目的とし、残domainの曖昧さと操作下界をBeam評価に使う。

別案として、未接続の目標出口ペアを1本つなぐ「複数タイルの一括回転」を近傍とする焼きなまし・反復改善を試す。
盤面は6ポート単位の次数2グラフとして管理し、各連結成分の出口・長さ・ボーナス・指定ペア一致を保持する。
経路候補は回転割当付きBeamまたはDijkstraで生成し、同一タイルへの向き要求が矛盾する候補を棄却する。
変更タイルが触れる旧成分だけを再分解し、接続数・経路得点・最終向きへの最小回転数の差を正確に評価する。
構築期は接続数の非減少を優先し、改善期は実スコア `K * (T - M * m)` による焼きなましへ切り替える。

接続数優先の構築状態とは別に、探索中に正確評価した全候補から実スコア最大盤面を保存する。接続数が多くても回転罰で0点になる盤面を最終出力に選ばない。
同じ接続数・0点の構築候補では経路長より回転数を優先すると、後の全接続や正スコア化につながりやすい。
差分評価では変更タイルの6ポートが属する旧成分の和が閉領域になる。旧matched寄与を引いて領域だけ再分解し、新寄与と回転差を足せば全盤面再計算と同じ値になる。

接続数非減少を各1経路変更へ課すと、既存経路を一時的に壊して複数ペアを再構築する遷移が消える。数本の既存経路を壊す ruin と、壊れたペアを順に戻す recreate を一つの大近傍として評価する。
経路候補Beamにも、その経路自身の価値だけでなく、触れる旧matched成分の破壊寄与と修復対象数を入れる。局所経路評価で全体的に有望な組み替えを落とさない。
一時状態ではmatched減少を許し、修復列の完了盤面を正確な実スコアで焼きなまし採否する。途中状態は最終出力候補にだけ残し、単調制約には使わない。

細い経路Beamでは片方向の取りこぼしが大きいため、各ペアを両端から生成する。候補回転コストは現在盤面との差ではなく、常に初期向きから最終候補向きへの距離を使う。
探索の周回数は固定せず、10秒制限から逆算した時間予算で決める。経路ビーム幅を狭め、少数候補を高速に生成して、多くの未接続ペアに対する一括変更を試す。

スコアを `k(t-Mm)` と分解し、外周幅1～3と特選経路0～3本を同じ外周構築から比較する。通常経路は深さ超過を二乗罰し、特選経路は `length*(bonus+1)-M*rotation` と完成盤面で失う接続を評価する。特選タイルを保護した後に通常ペアを短い順で再配線し、各追加直後の完成盤面を厳密評価して最良prefixを残す。

seed 1～10では特選本数の最適値は一定でなく、長大経路の追加で接続を多く壊すseedでは0～1本が優位だった。構築後は1～4タイルの同時回転を時間制御SAで探索すると、経路単位構築では届かない相乗的な変更を回収できた。全盤面の実スコアを常に最良解判定に使い、補助エネルギーはスコア・接続数・純得点だけに限定する。

外周先行型では、特選候補を通常ペア構築から予約しておき、短経路を貪欲に作った後で外周シェルだけを固定する。予約出口の周囲を外周幅+1だけ局所解放してゲートとし、内側は再び自由にして長大経路を作る。通常経路全体を固定すると内部が分断され、入口だけ開けても特選経路が完成しない。外周シェル固定・局所ゲート・内部解放の組合せなら、確保済み接続を概ね残しつつ内部資源を利用できる。

seed 1～10でゲート方式は `mean_log10` を 5.063469 から 5.087280へ改善した。特にseed 5は285880から376277、seed 8は828000から1005680へ伸びた。盤面内に非常に長いループが生じるとJava testerの再帰描画がスタック超過するため、最良盤面更新時に長さ400超の内部ループを除外する。

周期1x1・1x2・2x2・2x3を全列挙し、無限格子上のポート遷移が短い閉路でなく並進成分になる配置を抽出した。周期配置を特選Beamの優先向きとして使うと、特選経路単体の `length*(bonus+1)` が数倍になるseedがあり、周期構造による長大化は実際に機能した。ただし複数テンプレートを逐次Beamで試す方式はSA時間を削り、seed 1～10の `mean_log10` はゲート版5.087280に対して5.079178だった。次はテンプレートごとの再探索ではなく、ブロック境界の接続型と長さを事前計算し、境界DPでテンプレート列を一度に選ぶ。

内部周期を完全固定してゲート周辺だけ探索する方法も試したが、周期成分の出口と目標ゲートが一致しにくく、代表seedでゲート版を下回った。周期配置は単一パターンを敷くのでなく、境界ポート写像が異なるマクロを複数用意し、DPで入口から目標出口へ写像を合成する必要がある。

2x2マクロの全1296配置から、入口境界ポート→出口境界ポート、長さ、ユニークボーナス数、回転数、向き割当を列挙し、内部最大矩形を蛇行するマクロ列へ境界DPを適用した。内部区間自体はBeamなしで構成でき、seed 5では従来の特選値940に対して1064を得たが、代表seed 2・5・8の最終スコアはゲート版以下だった。DP状態が単一経路の局所価値しか持たず、外周接続の破壊 `Δk` を評価できないことが主因。特選Beamの訪問単位をタイルからポートへ変更すると、同じ向きの別接続再訪によりseed 8の特選値は最大5453まで伸びたが、探索時間増加と接続破壊で最終スコアは改善しなかった。

外周盤面の各matched pathを成分IDとし、各タイルへ「向きを変えると破壊し得る成分集合」のu128マスクを付ける。特選経路状態に破壊マスクを持たせ、`ΔS=Δk*Q+(k+Δk)*Δq` の予測値で順位付けすると、長さだけでなく既存接続維持を直接評価できる。これは `N>=16 && M<=2` で有効で、seed 1～10の `mean_log10` を5.087280から5.104032へ改善した。高Mや小盤面では回転・探索時間の損失が勝つため従来評価を使う。

特選Beamの回転罰を現在の接続数kに比例させる実験では、ほぼ実損失どおりの `k*M*rotations` 型が `mean_log10=5.094540`、弱めた `2*k*M*rotations` 型が5.092066となり、現行5.104032を下回った。特に小盤面・高Mで、回転を要する長経路がBeam途中で消え、後段SAの初期解も弱くなった。回転コストは部分経路へ一律に強く課すより、経路完成候補を多様に残し、完成盤面の真スコアで比較する方がよい。

通常ペアを外周深さ1、2、3の順に短いものから固定し、各固定前後で未処理ペアの楽観的ポート到達可能性を比較する層別構築を試した。新たに到達不能となる固定は拒否し、あらかじめ予約した1ペアだけを最後に内部全体で長く結ぶ。層別単独は `mean_log10=5.103709` で従来5.104032とほぼ同等だが、seed 4・5では改善した。層別候補2個と従来ゲート候補を6.5秒以内に構築し、真スコア最大の盤面だけを残り時間のSAへ渡すポートフォリオでは5.105000となったため採用した。

層別構築を深さ5まで拡張すると、深さ4・5で通常接続は増えた一方、予約した特選経路が完成しないseedが増え、`mean_log10=5.103164`へ低下した。楽観的到達可能性だけでは長経路用の十分な内部面積を保証できず、通常経路が内部を先に消費するため、外周層は深さ3までとする。

外周のfixedは構築中だけ使い、最終SAでは元々全タイルを自由に変更している。構築打切りを6.5秒から5.5秒へ早め、外周・特選盤面を初期解として扱う自由SAを約1秒増やすと、seed 1～10の `mean_log10` は5.105000から5.108523へ改善した。外周を厳密な最終構造として磨くより、十分な接続を持つ初期解を早めに作り、全盤面SAへ時間を渡す方が有効だった。

深さ1→2→3の各遷移でも前層のfixedを全解除し、領域限定SAを行う方式を試した。到達可能性だけで経路上書きを許す版は `mean_log10=5.064490`、各上書きを完成盤面の真スコア改善時だけ許す版も5.077358で悪化した。浅い層で得た多数の接続が少数の高得点経路へ置換され、後段SAだけでは接続数を回復できない。層間では短経路を固定して構造を保持し、全解除は外周・特選構築が完了した後の最終SAだけで行う。

最終SAに、対象ペアの両端から現在伸びている2経路成分と隣接1層を解放し、`find_route`で一括再接続する部分破壊再構築を混ぜた。25msごと・最大6msでSA受理する版は `mean_log10=5.098011`、100msごと・最大10msで真スコア改善時だけ受理する版も5.101924で、単純回転SAの5.108523を下回った。大盤面では再構築の受理が0～1回と少なく、局所経路探索に使う時間が損失となったため不採用。

経路上の1タイルと、現在の入出方向を避けた隣接三角形を選び、3タイル全216配置または4タイル最大480配置を列挙する局所延長近傍を追加した。対象ペアが同じ出口へ接続されたまま経路長が必ず増える配置だけを候補化し、上位8個を盤面全体で評価してSA受理する。全盤面で使うと5.108339、改善時だけ受理すると5.102172だったが、効果が安定した `N>=17` に限定すると `mean_log10=5.109282`となり、従来5.108523を上回ったため採用した。

大型盤面で局所延長が効く理由を確認するため、局所近傍を外して構築打切りを5.5秒から4.5秒、3.5秒へ早め、通常回転SAをそれぞれ約1秒、2秒延長した。`mean_log10` は4.5秒版5.108428、3.5秒版5.095271で、局所延長版5.109282に届かなかった。単なる通常SAの反復不足ではなく、1～3タイルの無方向な回転では到達しにくい「接続を保って経路を伸ばす整合的操作」を局所テンプレートが供給する効果がある。

総時間を9.2秒から18.4秒へ倍増し、局所延長を外して通常SAへ追加時間を渡した。温度期間も倍増する版は `mean_log10=5.094640`、元の約3.7秒で冷却後に低温継続する版も5.097493で改善しなかった。単一SAの追加反復は同じ局所谷で消費されやすく、計算量増加だけでは局所延長近傍を代替できない。

盤面サイズに応じて2～10枚のタイル集合を選び、75%を連結、25%を離散集合とし、変更枚数ごとに上位3状態を残すビーム山登りを試した。途中スコアが悪い複数枚変更も最後まで保持し、完成盤面の真スコア改善時だけ採用する。山登り単独は `mean_log10=5.104937`、従来SA70%＋山登り30%も5.104733で、局所延長付きSA5.109282を下回った。改善自体は多数得られるが、全盤面評価が重く、大型盤面では数百集合しか試せないため、差分評価などの高速化なしでは時間効率が不足する。

高得点盤面では同じタイルを大回りで複数回踏む経路が見られるため、特選Beamの訪問管理をタイル単位から内部ポート単位へ変更した。同じ内部線分の両ポートを使用済みにして逆走を防ぎ、別の内部線分なら同じタイルを最大3回通れる。再訪時は最初に決めた回転を強制し、回転コストとボーナスは初回だけ数える。全盤面で有効化すると巨大経路が外周接続を壊して `mean_log10=5.062580`だったが、改善した中型・低Mの `9<=N<=13 && M<=2` に限定すると5.113783となり、従来5.109282を上回ったため採用した。
特選経路のポート単位再訪について、`9<=N<=13 && M<=2` の両側・複合条件は seed 集合への過学習になり得るため廃止した。無条件再訪は `mean_log10=5.071556` まで悪化したが、計算量上の片側条件 `N<=13` のみとすると `5.117138` となった。M では分岐せず、小盤面では再訪型の長大経路を十分探索でき、大盤面では通常のタイル単位探索へ時間を集中する方針とする。
`S=kQ (Q=t-Mm)` では接続1本の限界価値が `Q` なので、特選Beamと最終SAで接続数をさらに強調する評価を比較した。破壊予測 `ΔS` を全ケースのBeam主項にすると `mean_log10=5.102754`、完成経路を現在盤面の真スコアで選ぶ版は5.114075、SAの仮想接続バイアスを `0.15Q` から `1.0Q`、`0.30Q`、終盤のみ0.30Qへ増やす版はそれぞれ5.106850、5.086139、5.113163で、現行5.117138を超えなかった。現行の真スコア自体が `Δk=1` を `Q` 点で評価しており、係数追加はQを育てる遷移を阻害する。接続数改善には評価の重み増加ではなく、接続を保ったまま新規接続を作る専用近傍が必要。
全ペア接続をハード制約にする6bit回転domain＋MRV Beamを再検証した。全タイルdomain版はseed 1で261ノード・全15接続を発見できた一方、N=20では45秒で深さ21/1141に留まり、現実装の全再計算伝播は大型盤面に使えない。現行解から未接続だけを直す増分版は既存経路を完全固定すると10 seedすべて追加0本、衝突経路を壊して再構築するトランザクション版ではseed 5を75→85、seed 8を80→81まで増やせたが、短経路置換でQが崩れ、全接続未達の中間解は大幅悪化した。中間解を破棄してもCSP時間の分だけ相対95%未満となった。

小盤面限定の完全CSPと「全接続→英雄のボーナス数→英雄長→回転→最終スコア」の辞書順も実装して比較した。seed 1は全15接続を達成し、英雄長を16→19へ伸ばせたが、回転が17→22へ増えてQが22→2、scoreが330→30へ悪化した。seed 1〜10のtester相対は86.67%。全接続CSPは構成可能性の証明として有効だが、長さを回転・Qより厳密に優先する辞書順はスコア式と一致しない。採用には、(1)差分domain伝播による大型盤面対応、(2)全接続完全解を多数保持、(3)英雄長ではなく全接続下のQまたは最終積でPareto選択、が必要。性能確認済みの本線コードは維持した。
接続本数の理想値を調べるため、総線分容量を `3×有効タイル数`、全ペアをポートグラフ上の最短距離で接続、余剰容量を最長の英雄経路へ集中、英雄だけが全Bボーナスを通過、回転罰0とする楽観モデルを計算した。接続中の2番目に短い非英雄経路を順に外して `距離-2` を英雄へ移す曲線では、seed 1〜10の全ケースで最大上限は `k=P`（全接続）だった。最初の1本を外すと全接続上限の90.00%〜99.10%となり、大盤面では差が約1%と小さいものの、このモデルでは接続削減による上限改善はない。詳細は `doc/theoretical_upper_seed1_10.md` と `src/theoretical_bound.py`。
単純容量上限を現実寄りにするため、独立最短経路が各タイルで要求する入出辺ペアを集計し、単一回転で同時実現できる最大3線分だけを同居可能とした。同居不能要求1本につき最小迂回2線分を通常経路へ加え、英雄容量から差し引く推定では、単純上限の86.2%〜96.2%となった。この補正後もseed 1〜10すべてで最大は全接続 `k=P`。ただし独立最短経路を各1候補しか使わず、迂回2の実現性も保証しないため厳密上限ではなく比較用推定値である。
各ペアに最大16本の同距離最短経路候補を持たせ、タイル回転の同居衝突を座標降下で最小化した。単一候補版から衝突はseed 2〜10で37%〜57%減り、推定値は単純上限の91.5%〜98.3%となったが、接続本数別曲線の最大は全10ケースでなお全接続 `k=P`。最短経路の共存を選択最適化しても、接続を捨てて英雄へ容量を移す優位性は見られない。
seed 1は3分のSA（約1.25億反復）でも既知520を更新しなかった。境界30出口が作る完全マッチングを利用し、k=13であり得る「正解2ペアだけを交差させる」210通りを、回転domain CSPで全列挙した結果も最大520（k=13, t=84, m=11）。k=14は完全マッチングの性質上存在せず、k=15の全列挙最大は465なので、520はk>=13の範囲で厳密最適。ただしk<=12を含む大域最適性は未証明。
回転重視のため特選Beam途中の回転罰を実効1/15から1/4・1/10へ強めたが、`mean_log10` は5.117138から5.098095・5.109450へ低下した。序盤の回転費だけを見て将来の長さを回収する状態を枝刈りするため、途中評価の強化は不採用。完成した局所延長候補の事前選抜だけを経路得点順から正確な `Δt-MΔm` 順へ変更すると5.116947でほぼ同等だったため採用した。完成経路順位と最終盤面判定は元から回転費込みである。
最終SAで入力時の初期向きを明示提案する近傍も検証した。複数タイル近傍の各提案を初期向きへ1/3・1/8で偏らせる版は `mean_log10=5.105805`・5.104681、独立した1タイル復帰を1/16頻度で混ぜる版も5.105090で、基準5.116947を下回った。初期向きへの単独復帰は回転を減らすが、そのタイルの3接続を同時に壊すため不採用。再検討するなら、復帰タイルの周囲2〜4枚を同時補修して接続を維持する構造化近傍にする。

最終SAの反復数を増やすため評価処理を計測・簡略化した。ボーナス訪問配列を毎経路初期化せずepoch stampで再利用し、各ポート・回転から次ポートへの遷移を事前計算した表で経路を追跡し、変更タイルだけから入力初期向き基準の回転数差分を更新する。遷移表版はseed 1で約1741万反復、seed 2で約23.9万反復となり、直前版比でおよそ1.7〜2.9倍、`mean_log10=5.124376`まで改善したため採用した。変更成分だけを再評価する方式も試したが、高得点盤面の経路成分が大きく、採用後の成分再構築も必要なためseed 2で約10万反復まで低下して不採用。

小盤面では高速化後に `Instant::now()` と温度の `powf` も無視できなくなったため、時刻・温度更新を256反復ごとにまとめた。seed 1の反復数は約1741万から2271万へ約30%増え、seed 1〜10の4並列評価は約28秒。単回スコアの `mean_log10=5.118592` は直前試行5.124376より低いが、焼きなましの軌跡差による分散があり、探索量を直接増やす低リスクな高速化として採用する。

最終SAの温度を `T=start^(1-p)×end^p` の明示的な指数減衰に整理し、高い開始温度を比較した。開始480・終了1.0は `mean_log10=5.112611`で、高温期に接続構造を壊しすぎた。一方、開始240・終了1.0は5.126762となり、従来の開始120・終了2.4の直近5.118592を上回ったため採用。高温化は有効だが、現近傍では480は過剰で240程度が良い。

次数2のポートグラフをLink-Cut Treeで管理し、長さと最大10個のボーナスをbit OR集約する完全動的評価を試した。内部ループだけ正確な全走査へフォールバックしたが、全ペアのsplay操作とループ成分処理が重く、seed 1は約2270万→25万、seed 2は約23万→7.7万反復へ低下したため不採用。代わりに各ペアの現在経路が通るタイルを `u128` マスクで保持し、変更タイルのマスクORで影響ペアだけ再評価する差分方式を導入した。採用時も影響ペアの経路だけ更新すればよく、全成分再構築は不要。小盤面は全評価の方が軽いため `N>=8` 相当の `W>=15` だけで有効化した。seed 2は約23万→37万反復へ増え、seed 1〜10は正確性の定期照合を通過して `mean_log10=5.130214`となり、直前5.126762を上回ったため採用。

差分評価後の追加高速化を順に検証した。通常SAの `undo`・提案向き・差分更新の3つの一時Vecをループ外で再利用すると、seed 9は約126万→139万、seed 10は約89万→93万反復へ増えたため採用。得点追跡と通過セル追跡を同じ関数へ統合し、セル列バッファのallocationとsortも除去した。一方、提案評価時にセル列まで常に保存して採用後の再追跡を消す版は、棄却候補への書込みが増えて反復数が安定しなかったため不採用。セル列は採用後だけ記録する。

構築上限を5.5秒から4.5秒へ短縮して約1秒を最終SAへ移すと `mean_log10=5.132224`となり、5.5秒版5.130214を上回ったため採用。構造化近傍を半頻度（局所延長60→120ms、復帰補修80→160ms）にすると5.111602、復帰補修だけ80msへ戻しても5.127157で悪化した。専用近傍は通常SA反復を消費するが解品質への寄与が上回るため、60ms・80ms間隔を維持する。

盤面を広く占有する2位・3位経路を縮め、最長経路へ容量を集中するため、構築後の最長接続ペアを英雄、次の2本を競合として固定し、SA受理エネルギーへ `weight*k*(hero_length-second_length-third_length)` を追加した。weight 0.20はseed 9で英雄1053・競合19/15まで集中したが `mean_log10=5.121082`で強すぎた。0.05でも経路長を別走査する版は反復数が半減して5.122653だったため、差分評価が元から追跡する経路長を保存して追加走査をゼロにした。高速版0.05はseed 9で942対20/25、seed 10で1487対11/119となり、`mean_log10=5.132510`で従来最高5.132224をわずかに更新したため採用。best盤面の保存は真スコアのみなので、集中バイアスで劣化した状態は出力しない。

英雄が全ボーナスを踏むと仮定すれば、長さLに対する英雄単体の回転上限は `floor(L*(B+1)/M)` と見積もれる。seed 1〜10の最終的な最長経路はすべて全ボーナスを踏んでいた。一方、この楽観値を特選ビームの途中評価へ入れると5.112669、全踏破完成候補の絶対優先は5.130731、5%完成ボーナスは5.131208、1%は5.129206で、基準5.132510を超えなかった。途中枝では全踏破が未確定であり、短い全踏破経路を過剰優先するため、真の採否評価には混ぜず、探索設計用の回転予算と診断値に限定する。

英雄伸長前に接続経路を1本ずつ短経路ビームで引き直す前処理を検証した。他経路を固定して全経路を縮める版は、長さ1090→520でも回転201→243のような悪い短縮を採り `mean_log10=5.106503`。英雄を保護し、非英雄だけを回転数非増加条件で300ms短縮する版は、各seedで採用0〜1本・短縮量ほぼ1線分に留まり5.127152だった。通常構築が既に短経路を作っているため単独再配線の余地は小さく、SA時間を奪うので不採用。容量を空けるには競合する2〜3経路を同時に解体・再構築する近傍が必要。

英雄への固定優遇を外し、SAを真スコア・接続数・純得点だけへ戻すと `mean_log10=5.127587`。英雄の実経路得点を補助評価に使う比較では、英雄得点から2位・3位得点を引く版5.130529、英雄だけ加点する版5.127944、英雄を `L(b+1)/(B+1)` で加点して競合は長さで減点する版5.129439だった。いずれも旧長さ集中版5.132510を超えないが、特定ペアへの過学習を避け、全経路を実際の `L(b+1)` で等しく扱う方針を優先して英雄補助は不採用とした。特選経路構築は良い初期解を作る役割として残す。

固定英雄を作らずボーナス込み経路得点を少数経路へ緩く集中させるため、SA補助評価へ `weight*k*sum(c_i^2)/sum(c_i)`（`c_i=L_i*(b_i+1)`）を追加した。二乗和は影響経路だけで差分更新する。weight 0.005は `mean_log10=5.128277`、0.01は5.132647、0.02は5.126595で、強すぎる集中は接続数・総得点を損なった。0.01を採用。特定ペアを選ばず全経路対称で、最終盤面は従来どおり真スコアだけで保存する。

最終SAで接続数減少を強く否定する評価を比較した。現在状態から1本減るごとに純得点Qを追加罰とする版は `mean_log10=5.125897`、1本でも減る遷移を完全拒否する版は5.124688、SA開始時kを下回る状態だけ2Qで罰する床方式は5.128125で、基準5.132647をすべて下回った。良い接続構造への組替えには一時的なk減少が必要で、受理評価の単調化は探索を閉じ込める。接続数改善は評価係数でなく、既存接続を維持した完成盤面まで一括生成する専用近傍で行う。

接続を壊す1タイル変更と復元変更を一括採用する近傍を比較した。中央＋隣接2枚の固定3枚全列挙は、後半だけでも `mean_log10=5.130666`、中央単独で実際にkが減る条件を加えると5.125248で、修復範囲不足だった。そこで、単独変更でkを落とした状態から、隣接およびランダム候補をビーム幅8・盤面依存深さ3〜10で追加し、元のk以上へ戻った完成状態だけを真スコア改善時に一括採用する可変長トランザクションへ変更した。結果は5.132118で基準5.132647とほぼ同等。接続損失を途中状態として許しつつ、悪化盤面をSAへ露出しない構造が有効。

可変長修復近傍の候補は、失われたペアを現在状態で再トレースした経路上に限定できる。その経路外のタイルを回しても当該ペアの遷移は変わらない。変更済みタイルからの経路距離が近く、既存接続への影響本数が少ない順に列挙し、セル添字の作業配列で重複除去を線形時間化した。候補評価も全ペア評価でなく、変更セルが元経路と交差するペアだけを `DifferentialEval` で再計算する。`mean_log10=5.132004` で全盤面候補版5.132118と同等を維持した。

修復候補列挙を変更集合から経路半径5〜12以内へ限定する版は `mean_log10=5.128865` で、通常SA反復数もほぼ増えなかった。大回り経路では遠方の修復点が必要で、局所半径制限は不採用。失われた経路の入口に対して出口を変えない回転を事前除外する版も5.126305で、候補順の変化による損失が大きかった。現状の主コストは候補列挙でなく候補ごとの影響経路再評価であり、次の高速化は経路トレース結果の共有・部分再計算が必要。

修復トランザクションの最初に回すタイルを、接続中の得点経路がちょうど1本または2本通るものへ限定した。0本は得点経路へ直接作用せず、3本は全線使用中で破壊リスクが高い。復元用の後続タイルまで同じ条件にすると `mean_log10=5.128681` へ悪化したため、制限は起点だけに適用し、復元時は3本タイルも使用可能にする。この版は5.132882で直前版5.132004を上回った。通過経路得点に比例した起点抽選は5.130049で、既に長い経路へ偏りすぎるため不採用。

後処理で回転数を減らす方法として「境界接続シグネチャ不変変換」を試す。局所領域の外周半辺が内部でどうペアリングされるかを署名とし、同じ署名を持つ配置間で置換すれば、盤面全体の出口間接続を保ったまま経路長・通過ボーナス・内部ループ・回転コストだけを変更できる。最小の非自明な領域は隣接3タイルの三角形で、1タイル・隣接2タイルには存在しない。下向き三角形 `A B / C` の `(2,4,0)` と、1枚だけ±1回転した `(1,4,0)`, `(3,4,0)`, `(2,3,0)`, `(2,5,0)`, `(2,4,1)`, `(2,4,5)` は外周12辺の接続が同一である。基準配置の内部三角ループを外側の通路へ挿入すると、その通路を局所的に長さ1から4へ伸ばせる。逆変換は接続を壊さず回転数を減らす後処理に使える。まず三角形を全列挙し、同一署名クラス内で真スコアを落とさず回転コストが減る置換を反復する。その後、4タイル菱形などについて `6^n` 配置から署名別の局所変換表を自動生成し、回転削減を第一目的、経路長・ボーナスを第二目的として適用する。

三角形の境界接続シグネチャ不変後処理を実装した。全三角形の216配置から現在と同じ外周12辺ペアリングを持つものだけを列挙し、接続数と真スコアを維持しながら総回転数が減る置換を反復する。seed 1〜10の初回は `mean_log10=5.135149` でseed 5・7などの回転を削減し、再測定は5.132098でseed 2・5・7・8において4・16・2・2回転を削減した。後処理時間は最大約40msで、SA時間を削らず実行できる。各変更は真スコア非悪化を検査するため安全な改善として採用する。

4タイル菱形にも同じ後処理を拡張した。菱形は隣接する三角形2個が1辺を共有する4タイル領域で、外周14辺のペアリングを署名とする。1296配置の同値類を幾何形状ごとに事前生成し、各領域では現在配置と同じ同値類だけを評価する。盤面上の幾何形状は3種類に集約できた。seed 1〜10ではseed 2・3・5・10で発火し、三角形後処理に加えて合計16回転を削減した。各置換は接続数を保ち、真スコア非悪化かつ初期配置からの回転数減少を条件とする。横一列3タイルと折れ線3タイルは216配置の署名がすべて異なり、非自明な不変変換を持たない。

局所変換は、回転数優先では経路得点を削って回転削減分と相殺する場合があるため、現在配置より最終スコアが厳密に大きい場合だけ採用する。同スコアなら経路得点や回転数にかかわらず何もしない。改善候補同士では `最終スコア > 経路得点t > 回転数の少なさ` の順で最善を選ぶ。seed 1〜10で `mean_log10=5.127572`、採用された後処理はすべて正のスコア差を持った。署名は接続関係だけを保証し、内部の経路長とボーナス通過は変わり得るため、後処理ログには `total_delta` と `score_delta` を残す。

最外周で両出口が同一タイルにあるペアを最初に直結し、その回転を固定する実験は不採用とした。seed 1〜10では対象ペアを全て直接接続でき、対応不能・同一タイル内競合・固定直後に個別の楽観到達判定がfalseになる他ペアはいずれも0だった。しかし `mean_log10` は5.103156まで低下し、再測定では最終接続数がseed 2で109相当から94、seed 5で74相当から62へ大きく減った。1ペアを直接結ぶこと自体は合法でも、その回転で同じタイルの残り2接続まで早期固定され、複数経路の同時両立性や長大経路の自由度を失う。個別の楽観到達判定ではこの損失を検出できない。

外周の深さ1を確定してから深さ2へ進む代わりに、最初から深さ2以内を一括構築し、その後を焼きなましだけに任せる実験は不採用とした。構築は0〜59msで、構築直後の個別到達不能は全seedで0だったが、長大経路を持たない初期解の多くは回転罰でスコア0となり、SA後も `mean_log10=5.070942` に留まった。seed 2/3/5/8などで接続数と長大経路得点が大きく不足した。2層一括構築自体は高速だが、凹凸の大きい評価関数上でSAだけから英雄経路を新生するのは難しいため、使うなら直後に長大経路の構造的構築が必要である。

`外周2層一括構築 → 英雄経路1本を構造的に構築 → SA` も不採用とした。英雄候補上位2本を比較した結果、ゲートなしで `mean_log10=5.096930`、予約出口周辺を解放するゲートありで5.089246だった。SAのみの5.070942よりは改善したが現行に及ばない。ゲートなしでは2層固定が予約出口を塞いで英雄経路が短くなり、ゲートありでは英雄経路が外周の短経路を破壊する。どちらもSAだけでは失った接続を十分に復元できない。2層一括方式を使うなら、英雄構築後に短経路を明示的に再構築してからSAへ渡す必要がある。

段階的な外周構築の深さをseed 1〜10の同条件で比較した結果、2段は `mean_log10=5.114560`、3段は5.133881、4段は5.107994で、現行の3段が最良だった。2段は英雄経路が伸びる場合もあるが通常経路の接続数が不足し、特にseed 9が弱い。4段は第4層で短経路数を増やす一方、固定領域が広すぎて英雄探索の失敗・短縮が増え、seed 5〜9が弱くなる。3段が接続数確保と英雄用自由領域の釣り合う点になっている。

境界シグネチャ後処理は、三角形と菱形を1回ずつで終えず、両方の改善が0になるか450msを使い切るまで交互反復する。順序比較では三角形先が `mean_log10=5.127218`、菱形先が5.125865だった。菱形先では10ケース全てで後続の三角形変換が0だった一方、三角形先では実際に改善が発火した。216配置で安い三角形を先に回収し、1296配置の菱形、その結果を受けて再び三角形へ戻る順を採用する。

三角形の同一境界シグネチャ変換を24msごとのSA近傍として使う実験は不採用とした。現在配置と同じ署名クラスの別配置へ遷移し、回転数減少には限定せず通常温度で受理した。seed 1〜10では1ケースあたり約230〜380回試行して0〜20回受理され、通常SA反復数はほぼ維持、最大実行時間9.28秒だったが、`mean_log10=5.126489`で改善が確認できなかった。終了後の厳密改善型シグネチャ後処理だけを残す。

英雄経路が通る三角形・菱形だけを対象に、同一シグネチャ内で回転増加とスコア悪化を温度受理しながら英雄を延長するSA近傍も不採用とした。100ms間隔・4ms予算で、英雄の生経路長増加を必須とした。序盤から使う版は `mean_log10=5.119694`、後半55%以降で候補スコア優先の版は5.126302、後半で延長量優先の版は5.125680だった。接続を保って英雄は伸ばせるが、回転コストと他経路の局所長を払い、探索状態を英雄延長へ偏らせる損失が上回った。終了後に最終スコアが厳密改善する変換だけを残す。

2本の特殊経路を同じ基盤上で共同選択する試作は不採用とした。最初に独立な候補プール同士を組み合わせた版は、共有タイルで要求回転が衝突し全seedで2本完成が0だった。次に1本目の候補を6本残し、各候補の回転を条件として2本目を再探索し、同一タイルの別線分だけを共有可能にした。さらに短い2本目を先に確保してから長い候補を探す二段階化で、2本完成とボーナスタイル1個の共有は確認できた。しかしseed 1〜10の `mean_log10` は独立版5.119766、条件付き版5.120857、短経路フォールバック版5.122224で、現行5.127472を超えなかった。完成例も共有ボーナス0〜1個が中心で、「共同幹線」ではなく長い1本と短い保険経路になった。複数英雄を実現するには、完成経路候補を逐次条件付けするのでなく、ボーナスタイルの回転domainと各経路が使う3線分を先に共有制約として置き、複数の現在ポートを同時に進めるBeam/CSPが必要である。

SAの補助評価 `sum(c_i^2)/sum(c_i)` は、同じ加重長合計でも少数経路への集中を優遇し、真の目的関数と一致しないため削除した。差分評価から二乗和の保持・更新も除去した。seed 1〜10の `mean_log10` は初回5.124911、再測定5.127102で、変更前5.127472と同等だったため理論整合性を優先して採用する。最終ログには全ボーナス踏破経路数、全マッチ経路のボーナス利用数合計、加重得点上位3経路を追加した。実測では全ボーナス踏破経路がseed 6・7・10で3本、seed 2・5・9でも2本存在した。以後は最大1経路の長さや得点集中度ではなく、全ボーナス踏破済み経路群の合計長と、ボーナス線分を利用するマッチ経路数を観測する。

seed 1〜100で構造指標を計測した。平均は接続率 `k/P=0.804`、全ボーナス幹線長率 `H/(3V)=0.707`、マッチ線分利用率0.935、ボーナス利用率 `bonus_uses/(3B)=0.735`、回転罰比 `Mm/t=0.131` だった。全ボーナス踏破経路は0本2ケース、1本53ケース、2本33ケース、3本12ケース。マッチ線分利用率は既に高く、空き線分・ループ回収だけの伸び代は平均6.5%しかない。一方で接続率とボーナス共有率は低いため、10%以上改善の中心は、低ボーナス長経路の線分を2〜3本の全ボーナス幹線へ移し、除去したペアを外周短路で再接続する線分再配分である。特にseed 4, 11, 25, 26, 30, 37, 43, 49, 57, 70, 72, 92, 93は線分利用率94%以上かつボーナス利用率60%未満で、単純延長でなく再配分が必要である。詳細は `doc/structure_diagnostics_seed1_100.md` とCSVに保存した。

経路を不足ボーナス数 `d=B-b` で分類し、本数・合計長・潜在利益 `Σ(Ld)`、欠損ボーナスmask、ボーナスタイルごとの利用経路IDと実通過線分数を診断へ追加した。seed 1〜10では長い `d=1` 経路はseed 2・3・5など一部に限られ、seed 4・7・8には有望な `d<=2` 経路がほぼない。seed 5の長さ約100の `d=1` 経路を全ボーナス化するruin–recreateを試したが、欠損ボーナス利用経路を1〜2本解放しても経路は見つからず、周囲半径8の部分経路を解放して初めて候補が出た。既存の全ボーナス英雄を保護すると候補は再び0になり、英雄まで解放した完成候補は再接続に失敗して最良でも `Δk=-2`、大幅スコア減だった。したがって「最後の1ボーナス追加」は独立な局所近傍ではなく、既存英雄との大域的な線分交換である。次は既存英雄と受取候補を同時状態に持つ2幹線再構築と、解体ペア群の外周再接続を1トランザクションで探索する。

`d=1`経路と欠損ボーナスを通る別経路の共有2点で中間区間を交換する2幹線クロスオーバーを試した。欠損ボーナスを含む区間だけに候補を絞り、第三線分修復用の隣接1タイルも同時に全列挙した。seed 5では対象を全ボーナス化する候補が2万件以上あり、`k`を維持する配置も存在したが、最良でも真スコアは大幅減だった。seed 1〜10では採用0、`mean_log10`も5.12578から5.12253へ低下したため不採用。未マッチ経路だけを交換相手にすると、欠損ボーナスの前後に必要な2共有点が0だった。既存マッチ経路を使うと全ボーナス化できるが、ボーナス区間を複製せず移すだけなので別経路の長さ・ボーナスを失う。数タイル回転ではなく、空き線分を欠損ボーナスから受取経路まで延ばす経路区間マクロを状態に持ち、2幹線を同時ルーティングする必要がある。

## 区間CSPオラクルと接続追加大近傍

Z3による区間CSPオラクルを作り、領域外の接続を固定したまま領域内の回転を一括決定した。`d=1` 経路の最後のボーナス追加については、seed 1〜10中6ケースに対象があったが、より高価値な既存経路の局所接続を完全維持する幅1の問題は6/6でUNSATだった。seed 5では幅2・3へ広げてもUNSATだった。最後の1ボーナス追加も安い局所操作ではなく、既存幹線自身を迂回させる連鎖交換が必要である。

一方、未接続ペア両端の現在経路間に最短回廊を作り、幅1領域で既存マッチ経路を全復元しながら新規接続するオラクルは明確な正例を得た。seed 1〜10の距離上位候補80件中、可行51件、真スコア改善12件、改善seedは6/10だった。seed 5では6タイル変更により `k=80→84`、`score=418320→433860` の解も見つかった。

本番では短い未接続ペア上位10件を対象に、最短回廊＋隣接1層（最大9タイル）を作る。領域内の変更集合をビームで成長させ、途中の接続減少は許すが、完成状態で `k` が増え、かつ真スコアも増える場合だけ一括採用する。差分評価には領域を通る既存経路maskと対象未接続ペアを含める。25ms予算を450ms間隔でSA後半に試す設定は、seed 1〜10で `mean_log10=5.134893 / 5.133118`、従来の5.126189を2回とも上回ったため採用する。

接続追加近傍だけをコンパイル時にON/OFFし、seed 1〜100を同一tester条件でpaired評価した。OFFは `mean_log10=5.124761`、ONは5.133042、paired差は+0.008280で幾何平均スコア約+1.93%。66勝5分29敗、中央値も+1.10%、符号検定は `p=0.000186` だった。N=3はほぼ不変、N=20は6件で-0.52%だったが、全体傾向は十分強いためN分岐せず本線採用する。

発火診断として、近傍呼出数に加えて対象pair試行数、対象接続を完成したビーム状態数、採用数を集計する。採用イベントは `(pair, Δk, Δt, Δm, Δscore, 領域面積, 変更タイル数, 一時破壊最大数)` を標準エラーへ最後に一度だけ出す。診断追加後のseed 1〜10は `mean_log10=5.136948` で速度・性能低下は見られなかった。実例では領域7・変更4枚・一時破壊1本から `Δk=+2, Δt=-21, Δm=+4, Δscore=+10315` を得ており、`Q+(k+1)ΔQ` に従ってQの小幅低下を接続増で上回る交換が機能している。

AHC010型のpath spliceも試した。matched経路上の2状態間を切断し、対象区間のポートだけを解放、他matched経路のタイル内ペアと使用ポートを固定した。最大40ステップ、同一タイル再訪可・同一ポート再訪不可のbeamで迂回し、完成盤面の真スコアが改善する場合だけ候補化した。10秒版は頻度型が `mean_log10=5.134120 / 5.134021`、終盤1回集中型が5.133629で、基準5.136948を下回った。探索力不足との切り分けのため18秒のON/OFF pairedを2組測り、1組目は `5.141108-5.139038=+0.002069`、2組目は `5.138156-5.139512=-0.001356`、平均差は約+0.00036（約+0.08%）だった。正改善候補自体は生成できるが、現実装では消費時間に見合わないため無効化する。再検討時はbeam nodeのVec clone削減、経路占有制約の前計算、複数回廊の共有探索が先である。

2～4タイル連結領域を境界transition（境界ポート間pairing）で圧縮する表を実装した。全 `6^n` 配置を同一pairingで分類し、現在配置と同じクラス内から、初期からの回転数・各境界通路の内部長・局所ボーナスmaskのPareto候補だけを試す。境界pairingが不変なので盤面全体の出口接続は変わらず、領域を通る経路だけを差分再評価し、matched数不変かつ真スコアが正に改善するときだけ採用する。2タイルには非自明候補がなく、採用は3・4タイルの双方で発生した。seed 1～10の同一コードON/OFF pairedでは `mean_log10` 差が `+0.000998`（幾何平均約+0.23%、6勝1分3敗）。差分化後は最大盤面の約6万領域・数千候補を扱え、後処理枠を220msから100msへ縮小した。これは重い探索を増やすより、transition同値類の事前列挙を状態圧縮として使う例である。

separating-transitionの最小の安全な枝刈りとして、固定タイルだけで誤った2出口が直結済みかを判定した。これは未固定タイルの全回転を同時Unionする楽観判定が理論上見逃す不可逆状態だが、seed 1～10の外周構築候補では追加棄却0件だった。経路追加処理が対象出口への到達を確認しながら固定するため、この種の誤接続は実際にはほぼ生成されない。効果のない判定は本線へ残さない。より強いcutには複数経路の同時容量制約が必要だが、毎候補の多品種flowは費用が大きいため、現時点では優先しない。

通常SAのorientation変更をtransition単位へ寄せる実験も行った。matched経路が1～2本通るタイルを優先し、使用中ポート対が変わる遷移先だけを候補化し、同じ遷移先を作る向きでは初期向きから最小回転を選んだ。75%誘導は `mean_log10=5.127032`、25%混合も5.130769で、通常SAより悪化した。使用transitionの重複提案は減るが、現在経路を壊す提案へ偏り、0本タイルを介した大域的接続変更も減る。候補抽選コストで反復数も落ちるため不採用。orientationは出力状態として維持し、transition表現は経路追跡・同一境界置換のように構造が保存される操作へ限定する。

境界transition表の使用実態をseed 1～10で計測した。非自明同値類は2304領域で参照され、raw代替8990件に対してPareto後も7670件（85.3%）残った。局所descriptorだけで現配置をPareto支配する領域は0だったが、真スコア正候補は24件、実採用は15領域あった。採用内訳は回転削減だけが1件、経路価値増が14件で、合計 `Δt=+198, Δm=+14, Δscore=+12978`。3タイルは `Δscore=+5864`、4タイルは+7114で双方に寄与がある。したがって表は単純な回転後処理ではなく、同一境界pairing内で内部長を大域的に価値の高い経路へ再配分している。一方で改善密度は低くPareto枝刈りも弱いため、全Beam状態へ汎用macroを大量展開するのは費用対効果が悪い。macro化するなら対象経路IDとbonus価値が既知のroute Beamに限定し、経路固有の増分で候補を絞る。

特殊経路route Beamへ三角形・4タイル菱形を2～4手まとめて抜けるmacro transitionを追加した。全体18秒・構築9秒・特殊経路枠約2倍の同条件でOFFは `mean_log10=5.139659`、単純ONは5.130705（約-2.04%）。macroは特殊経路値の最大を1320から1806へ伸ばしたため到達能力はあるが、重複状態と数手先行した評価が通常状態をBeamから排除した。重複除去と通常75%・macro25%の別枠を設けたv2も5.133034（OFF比約-1.51%）で負だった。一部seedの長大経路は伸びても後続ペアとの両立性を評価できず、全体スコアを落とす。route Beamへ局所macroを混ぜる設計は不採用。再検討するなら深さではなく消費線分数を揃えた層管理と、後続接続容量を含む評価が必要である。

外周の通常経路がボーナスタイルを先に使わないよう、通常route rankへ `penalty * bonus_count` を追加した。特殊経路評価は変更しない。seed 1～10の初回は罰点0が `mean_log10=5.134415`、120が5.132251、300が5.139183、600が5.137119。時間揺らぎを確認するため0と300を再測定し、2試行平均は0が5.133091、300が5.135291、paired差 `+0.002199`（幾何平均約+0.51%、9勝11敗）だった。300では代表試行の平均接続率は0.88037→0.88025でほぼ不変、回転数220.6→216.7、純得点Qは4502.6→4560.5。最終ボーナス利用率は0.838→0.796へ下がったため、効果は利用本数増ではなく、低価値な短い外周経路のボーナス利用を避け、長い経路へ線分と回転自由度を残すことにある。弱い罰では経路を中途半端に歪め、強すぎる罰では迂回損が増える。効果は中程度で勝率も高くないため、100 seed評価で負なら再検討する。

SA終了温度を `0.2 / 1.0 / 3.0 / 10.0` で比較した。初回の `mean_log10` は順に5.137816, 5.122102, 5.135025, 5.116198。区間別計測では終了温度1.0でも終盤1/4の悪化受理はほぼ0だが、第3区間の悪化受理率が13.0%あり、0.2では0.97%まで早く冷える。0.2と1.0を再測定した2試行paired差は `+0.008359`（幾何平均約+1.94%、11勝3分6敗）、振れの大きいseed 1を除いても+0.001720（約+0.40%）。さらに0.05と0.01を試し、0.05は単発5.132956で弱かったが、0.01は5.135697と5.139796。計測カウンタなしの2試行平均は0.2が5.136266、0.01が5.137746で、paired差 `+0.001481`（約+0.34%、11勝3分6敗）となった。現問題では後半まで状態移動を残すより早めに局所改善へ切り替える方が安定するため、終了温度0.01を採用する。計測カウンタは反復速度を戻すため提出版から除去する。

低温域で探索状態を真スコア最大の保存盤面へ戻す実験を行った。進捗75%で1回復元する版では10/10ケースすべて `current.score == best.score`、65%・85%の2回版でも20/20回すべてscore差0だった。同点で `Δt=+4～6, Δm=+1～2` の別表現へ戻る例だけあり、スコア回復はない。終了温度0.01では65%時点ですでにほぼ山登りとなり、探索状態自身が真のベストスコア上にいる。復元は差分評価器再構築コストと同点状態の多様性損失しかないため不採用。

最終盤面の線分は、matched terminal path、invalid terminal path、internal loop の3種に完全分解でき、その合計は必ず `3V` になる。タイル回転は6ポートを常に3組へ割り当てるため、物理的な未割当線分は常に0である。改善余地として見るべき「空き」は、得点に寄与しない invalid path と internal loop の線分合計である。診断ではスコア式 `k*(t-M*m)`、全matched経路、全invalid経路、全loopと、各分類の線分数をstderrへ出す。

現在解の交互サイクルから2～3目標ペアを選び、近距離の仮想ペアリング回廊と最高価値matched経路への最短接続回廊を同時に最大9タイル変更するBeamを試した。仮想短絡を完成した状態はseedによって36～2283件生成できたが、seed 1～10の真スコア改善・採用は0件だった。誤接続だけを短くすると余剰線分はloop/unmatchedへ戻り、英雄へ自動移管されない。次は英雄経路上の2点を明示的に切断し、その両端・誤接続区間・仮想短絡を同時に再接続するpath-splice型CSPが必要。局所回廊版は不採用としてコードから除去した。

matched経路2～3本を完成解から後処理で犠牲にして全ボーナス英雄へ線分移管する候補を、`G_r=ΔQ-rQ/(k-r)` で楽観評価した。各経路の実長・実ボーナス数を使い、仮想誤接続長 `Ls` は元ペアを禁止した4/6出口完全マッチングを全列挙し、六角セル距離+1の総和で下限評価する。単独供給価値上位48経路から最大18,424組を調べたseed 1～10では、正候補は1ケース1件だけだった。その候補も回転増0・経路干渉なしを仮定した `gain_q_after_k=+24.44` に留まる。別試行の上位28版でも正候補は1ケース2件、最大+7.58だった。これは完成解を局所的に壊す後処理の枝刈りには使えるが、外周競合・他ペア接続率・hero占有まで変える初期構築の仮想matchingを否定する指標ではない。

探索地形比較として、baseline、遠距離2ペアを距離最小の別matchingへ置換するr=2、3ペアを置換するr=3を実装した。最有力hero候補は犠牲対象から除き、各系統は仮想matchingによる構築から元の真ペア評価によるフルSAまで独立に9.2秒探索する。距離だけで候補を選んだ2巡では、r=2/r=3単独平均はbaselineを下回ったが、3系統oracleはbaseline比 `mean_log10 +0.006752`、幾何平均約+1.57%だった。さらに距離上位8組を通常route beamで二次評価して外周消費線分を見積もると、別試行のoracle差は `+0.011645`、約+2.72%となり、10ケース中baseline 6件・r=2 3件・r=3 1件が最良だった。仮想接続は一律採用ではなく、多始点として有望。次は3系統を本番10秒内で構築・選別する方法が課題。

3系統を本番10秒へ圧縮するため、短い独立構築、各220msまたは150msの真スコアpilot、勝者への残時間SAを試した。構築時間を `[1800,1100,1100]ms` とした版は `mean_log10=5.127761`、baseline約5.1348を下回った。baselineを厚くした `[2400,700,700]ms` 版も5.129701、小盤面だけ3系統とする版も5.133048で明確な改善はなかった。長時間oracleの利得より通常盤面で構築を3回行う機会損失が大きく、短いpilotは長時間の勝者予測にも不十分だった。通常実行はbaselineへ戻し、`MM166_VIRTUAL_R=2/3` で単独系統、`MM166_VIRTUAL_AUTO=1` で3系統自動比較を再現できる実験モードだけ残す。今後使うなら構築の共有・高速化、または候補選別精度の改善が必要。

現行本線と分離した `HexTiles_congestion.rs` で、混雑料金付き逐次ルーティングを実装した。各セルのコストを基本長、使用本数の一次・二次罰、回転費、既存orientationを壊すsoft-lock費、ボーナス予約費から作り、静的距離上位窓の中で軽量beamによる実コストが最小の本来ペアを順次接続する。最後の2～3ペアをabandonし、英雄構築後に元matchingを禁止した最短完全matchingで誤接続する。常時soft break版は `mean_log10=5.099088` で本線5.134158を下回ったが、seed 5だけは415600から462000へ約+11.2%だった。候補窓・beamを軽量化した版は5.095台、通常構築を既存orientation互換ルートだけに制限し、破壊を英雄・再matching段階だけにした版も5.097286だった。互換版は逐次接続中のk維持を改善したが、英雄へ十分な連続領域を残せず、10ケース最終値では本線に勝てなかった。混雑料金だけでは内部予約の代用にならないため、次に発展させるなら英雄用回廊の明示予約、途中盤面の複数snapshot保持、abandon判定と英雄領域を同時に扱う必要がある。

外周深さ3以内で英雄以外の未接続を最大2本、英雄込み最大3本へ抑える実験を追加した。各変更後に全本来ペアを再追跡し、壊れた経路を未処理へ戻すrip-up/rerouteを行う。最長3ペアを除いた最短距離下限と外周3層の線分容量を比較すると、10ケースすべてで必要量は容量の約26～56%に収まり、単純容量は障害ではなかった。逐次法だけでは未接続が `2,7,34,26,9,16,3,35,20,14` 本残った。そこで外周全タイルを対象に `matched数 → invalid terminal path総長 → 回転数` の順で最適化する外周専用SAを追加し、一様1タイル近傍では `0,10,0,10,9,0,2,14,8,6`、`mean_log10=5.125309` まで改善した。未接続経路上のセルを85%選ぶ版は大型盤面を `0,8,9,0,7,0,4,14,5,4` まで縮め、平均未接続数は改善したが3本以内は3/10ケース、最終 `mean_log10=5.114705` だった。複数ランダムタイル近傍と集中率55%はともに悪化。全ケース3本以内には、未接続terminal path同士の所有線分を同時交換する交互路・区間再構築近傍が必要。

外周3層最適化後へ本線の長いボーナス経路構築と最終SAを合成した。外周盤面を共通土台として特殊経路1・2・3本を従来のspecial Beamで構築し、周辺通常経路を再構築する。`k>=P-3` の候補を優先し、同条件内では真スコア最大を選んで本線と同じ最終SAへ渡す。seed 1～10は `[520,869448,434412,65538,491202,86784,56025,904475,209152,305544]`、`mean_log10=5.124966`。外周集中版5.114705から `+0.010260`、幾何平均約+2.39%改善し、本線5.134158との差は約-2.1%まで縮んだ。特殊経路構築直後に`P-3`を満たす候補は大型盤面ではほぼ無く、一度kを落としてから最終SAが回収する形だった。外周接続と長経路を両立するには、special Beam中で外周経路の破壊価値をより強く扱う必要がある。

本線SAの通常回転近傍を現在向きから±1だけに制限した。±1回転はタイル内3接続のうち1接続を保存するため、ランダムな全回転より破壊的でない。全サイズ一律ではseed 1が506から351へ崩れ、`mean_log10=5.120662`、本線比約-3.1%だった。一方seed 2～10だけでは幾何平均約+0.61%だったため、有効タイル数80以下だけ従来の全回転、それより大きい盤面は±1とした。seed 1～10の2試行は5.133618と5.135795、平均差は本線5.134158比 `+0.000548`、幾何平均約+0.13%。効果は小さいが非悪化傾向として採用する。構造的修復近傍の全回転列挙は維持する。

出口セル間の六角最短距離が閾値以上の元ペアを長距離候補とし、5本以上ある場合は初期構築用の目標matchingを仮想的に付け替える。元ペアを2ポートノードとして2～4ノードの巡回順・向きを全列挙し、元matchingと合わせて閉じた交互サイクルになる誤接続を作る。逐次貪欲を幅192のBeamへ拡張し、状態に未処理ペア、残す1～2本、確定サイクル、仮想距離を持たせる。各状態では現在ノードを含む平均距離上位48サイクルを展開し、最終仮想距離が最小の組合せを選ぶ。閾値比較は`0.75N=5.125795`で7/10ケースに発火して明確に悪化した。3巡平均は`1.0N=5.136639`、`1.25N=5.136222`で差は約0.01%と小さいが、より多く発火して僅かに高い1.0Nを既定値に採用する。3・4ノード強制は5.116587まで悪化したため、Beam内で2ノードより有利な場合だけ選ぶ。`MM166_LONG_THRESHOLD_NUM/DEN`で閾値、`MM166_DISABLE_LONG_VIRTUAL=1`で従来構築へ戻せる。

仮想matchingの採否を、距離だけでなく接続数損失と比較する盤面別価値判定へ変更した。長距離ペアを距離降順に並べ、最長1本は必ず英雄候補として保持する。2位からの各prefixについてBeamで最短の2～4ノード交互サイクル分割を求め、`gain=節約線分数×B` と `loss=r×Q_est/(P-r)` を比較する。`Q_est`は全ペア最短接続後の残線分を最長ペアへ集約し、概算回転罰を引いて求める。利得係数1/1.3/1.5/2/4を比較し、係数1は対象4ケースを全棄却して2巡平均5.139110、1.3は高ボーナス1ケースだけ採用して5.135440、1.5は2ケース採用して2巡平均5.138077、2は5.136020、4は5.133051だった。緩和するほど悪化したため既定係数は1とし、本当に推定利得が接続損失を上回るケースだけ採用する。`MM166_VIRTUAL_GAIN_COEFF_NUM/DEN`で再調整可能。

長距離ペアの保護本数を最長1本から上位3本へ増やした。既定係数1では候補がほぼ棄却され比較不能なため、仮想化が発火する係数1.5で比較したところ、1本保護の2巡平均5.138077に対し3本保護は5.133319、幾何平均約-1.09%だった。2・3位の長距離ペアまで供給側から外すと、残りだけでは節約効率の高い短絡を作りにくいため不採用とし、最長1本保護へ戻す。

最長1本だけを予約して外周短路を作り、そこで自然に接続できなかったペアを追加の長経路へ昇格する構築を試した。失敗後の誤接続腕長順は5.120079、未接続中の出口間距離順は5.135762、上位失敗候補を1本ずつ独立に伸ばす版は5.130595、最長1本用に作った同一外周盤面の空き領域へ伸ばす版は5.125883だった。従来の固定長距離候補版2巡平均5.139110を超えない。短路構築後の空き領域は分断され、失敗ペアは既存外周経路との競合が強いため、special Beamで長く再構築する対象には不向き。不採用として元へ戻し、未接続ペアは長経路候補ではなく接続修復対象として扱う。

短い本来ペアを外周3層で先に仮固定し、その残余盤面上で長距離ペア各端点間の実routeコストを測り、幅192のmatching Beamで犠牲候補と誤接続先を選ぶオラクルを試した。仮固定盤面をそのまま完成させる版では発火2ケースの候補真スコアが0と10920に留まった。仮固定は候補選択だけに使い、選択後は通常構築からやり直す版もseed 1〜10で `mean_log10=5.133502`、最大実行時間9.782秒で、基準2巡平均5.139110を下回った。個別routeコストは複数の仮想経路間の競合を表せず、選別用盤面で得た節約を通常再構築でも再現できないうえ、400msの測定費が重い。不採用としてコードは元へ戻す。再検討するなら、候補選択と短路・誤接続構築を同じトランザクションで完成させる必要がある。

`connect_repair`を、未接続ペアの両腕間の最短回廊に触れるmatched経路からdestroy costの小さい2〜5本を小Beamで選び、対象ペア→高価値な剥離経路の順で再配線するrip-up/rerouteへ拡張した。回廊＋隣接1層だけを解放する版は10秒で3/10ケースに採用され、`Δk=(+1,+2,+1)`、`mean_log10=5.139563` と基準5.139110をわずかに上回り、構造自体は成立した。しかし18秒pairedではON 5.142058、OFF 5.142669で約-0.14%、ON採用0件となり、長い通常SAが同種改善を先に回収した。復元失敗を1本許して残余誤接続へ送る版は採用が出ても5.134146へ悪化した。完成候補数は十分なので探索時間不足ではなく、通常探索との重複とQ損失が問題。現形は不採用としてコードを戻す。再検討するなら単発targetではなく複数の未接続ペアを同時追加し、残余matchingへ送る損失を複数の`+k`で補償する必要がある。

2〜3本同時追加型rip-up/rerouteも長時間オラクルで検証した。未接続ペアの両腕間最短回廊を作り、回廊同士が同じmatched経路を共有して壊せる組だけを `共有競合数/回廊長合計` で順位付けした。共有経路を中心に最大5本を剥離し、追加対象2〜3本→剥離経路の価値順で再配線した。20秒、seed 1〜10で候補群を延べ1,502回調べ、完成状態439件を真評価したが、`Δk>=2`かつスコア改善は0件、採用も0件だった。完成候補数は十分なので探索不足ではなく、複数追加でも共有破壊による自由度より剥離・再接続のQ損失が大きい。このrip-up/reroute系列は打ち切り、コードを元へ戻す。

外周1〜3層をペア順貪欲でなく全体状態として持つglobal Beamを試した。状態はorientation/fixed、接続mask、外周線分長を持ち、短い未接続ペアと利用層を遷移にした。評価は確定接続数、外周長、回転、未接続出口周辺の固定圧、hero用内向きゲート数。初版は層分岐過多で20秒 `mean_log10=5.131925`。最大層だけ・幅12・候補6へ高速化すると5.137782、従来20秒5.138065とほぼ同等だった。暫定真スコアでgreedy/globalを選ぶhybridは20秒2巡でON 5.140704、OFF 5.138383。正例はN=6のseed 4、幅3・予約なし候補だけで、外周状態が `k=29,Q=138` から `k=30,Q=1120` へ改善した。対象をその1回へ限定するとseed 4単独でOFF 65175、ON 65868だった。しかし通常seed 1〜10最終評価は5.137652で現行基準2巡平均5.139110を下回り、10ケース中の実質1正例だけでN帯を限定するのは過学習リスクがあるため不採用。コードを戻す。再検討時は100ケースでN帯ごとのglobal採用率と最終改善率を先に測る。

提出コードをクリーン化した。既定で使っていなかった仮想matchingの手動r=2/3系統・3系統auto比較、全ON/OFF環境変数、機能有効化定数、犠牲ペア全列挙診断を削除し、現在の既定解法だけを直線的に実行する構成へ整理した。`src/`の代替ソルバー、バックアップ、seed専用オラクルも削除し、公式`sample.rs`と提出本体だけを残した。クリーン版seed 1〜10は `mean_log10=5.140324` で、整理前の既定動作と同等だった。

追加整理で詳細解診断、楽観到達可能判定、長距離仮想matchingを同時に削除したところ、seed 1〜10が `mean_log10=5.121059` まで約4.3%悪化した。診断と仮想matchingはこの10ケースの構築を変えないため、原因は楽観到達可能判定だった。外周層の固定数が変わりseed 6・9などが崩れる。到達判定だけ戻すと5.139122へ復帰したため、これは棄却件数だけでは効果を測れない構築制約として維持する。最終的に詳細診断と長距離仮想matchingは削除し、到達判定だけ残す。

六角盤面のセル距離は `max(|dr|, |dc|, |dr+dc|)` を使う。従来の `|dr|+|dc|` は方向 `(1,-1)` さえ距離2と見なし、route Beamとペア順を方向依存に歪めていた。route Beamのヒューリスティックだけを正す実験はseed 1〜10で旧式と同等以上だった。一方、通常ペアの接続順も正距離だけへ変えると悪化した。外周弧、同距離候補の分散、外周帯BFS、出口方向と初期回転数を含む最短路推定も改善しなかった。旧マンハッタン順の強さは正しい距離ではなく、対角方向のペアを偶然後回しにする順序効果だった。今後は距離を誤りへ戻さず、残り自由度や競合を明示的に評価して接続順を作る。

現在matchingと目標matchingの和集合の交互サイクル数も計測した。SAへ256反復ごとの厳密サイクル差を加えると `mean_log10=5.11897` まで悪化した。最終状態ではサイクル数が概ね `matched+1〜3` で、接続数以外の勾配がほとんどない。局所回転は既存のmatched成分を併合する変化が多く、誘導項はそれらを拒否するだけだったため不採用とした。サイクル数はstderr診断だけ残し、将来サイクル全体のrip-up/reconnect候補を作る際に利用する。

距離修正の採用形では、幾何距離を正しい六角距離として独立させ、旧マンハッタンとの差分だけを通常外周の `diagonal_contention` として明示した。対角ペアは通常接続では後回しにし、同じ競合コストが大きいペアをhero候補として優先する。route Beamは純粋な六角距離を使う。seed 1〜10は `[520,932386,546014,64604,483276,85698,53110,1125657,195362,308112]`、`mean_log10=5.140636` で、クリーン旧基準5.139122をわずかに上回った。

旧マンハッタン `|dr|+|dc|` の意味を調べた。正しい六角距離との差は、`dr,dc` が逆符号のときだけ `min(|dr|,|dc|)` であり、NE/SW方向の1歩を2として数える重み付き六角距離である。seed 1〜10では720ペア中217ペア（30.1%）に罰が入り、大型盤面では平均順位が1ペアあたり約6〜8位置動き、最大52位置後退した。全回転は一様生成なので問題自体にこの軸の特別性はない。同じ重みを120度回した軸0/1は `mean_log10=5.111602/5.115683` と悪化し、現軸5.140636だけが強かった。これは純粋な距離精度ではなく、外周の出口列挙順、同距離タイ、逐次固定、seed 1〜10への長期調整が作った探索上の軸依存である。したがって汎化性は未確認で、100 seed評価時には軸別比較が必要。

全ペアの幾何最短経路から静的混雑度を作り、ペア順へ利用した。最初は最短経路に含まれ得る全セルへ1票を入れたが、経路選択肢が多いペアほど過大評価したため、`ways(a,v)*ways(v,b)/ways(a,b)` による一様最短路の通過確率へ修正した。それでも混雑を主キーにする版は `mean_log10=5.053097`、距離への加算は5.128136、同距離内の空き優先は5.126492、混雑優先は5.119231で、基準5.140636を下回った。幾何最短路は外周層制約・タイルpairing・逐次固定後の実混雑と一致しない。空きペアを先にすると長い柔軟経路を早期固定し、混雑ペアを先にすると競合域を早期消費する。静的混雑順は不採用とし、再検討するなら現在domain上の実route候補数や将来domain減少量を動的に測る。

経路が要求するのはタイル角度ではなく `(enter,out)` 接続なので、回転を即固定せず6bit domainで保持する構築を追加した。接続によっては2回転を残し、後続経路の要求とのintersectionで初めて1回転へ絞る。全候補をdomain化すると通常経路が同一タイルへ詰まり `mean_log10=5.096998`、layeredだけでも5.130895だった。そこでlayered 2試行を従来即固定1本＋domain保持1本に分け、legacyは即固定のまま残した。domain構築後は残った複数候補を真スコア貪欲で具体化する。seed 1〜10の2回評価は `mean_log10=5.142420/5.142833` で、基準5.140636を再現して上回ったため採用する。

## 角度0の水平レーン縮約

全タイル角度0では接続が `(NW,NE),(E,SW),(SE,W)` となる。右向きに追うと移動方向が `NE→E→SE` の周期3で繰り返され、3タイルの変位は `(0,+2)` になる。内部経路は隣接2行から出ず、左右へ単調に進む水平レーンへ分解される。折り返しや内部ループはなく、斜辺付近だけ上辺・下辺の出口へ抜ける。

これは全タイルを実際に0へ回す初期解ではなく、探索用の基準トポロジーとして使う。角度0レーン上の3接続の回転domainは、`NW-NE={0}`、`E-SW={0,5}`、`SE-W={0,1}` である。したがって水平レーンを3タイル進むうち1タイルだけ角度0に固定され、残る2タイルは2択を保持できる。レーン区間には角度でなく必要接続domainを設定し、後続経路や回転コストに応じて具体化する。

縮約グラフではノードを角度0の水平レーン、通常遷移を同一レーン上の移動、角度変更タイルを隣接レーン間のスイッチとする。特に角度±1は角度0の接続を1本保存し、残り2本を組み替える2レーン乗換スイッチとして扱える。各ポートの基準レーンIDと、各タイル・各回転によるレーン間遷移を前計算し、`(lane,port)` 上で最小スイッチ経路を探索する。通常ペアは少数スイッチで短く接続し、主幹線は複数レーンを乗り換えて内部・ボーナスへ進入させる。

注意点は、長い基準レーンでも3タイルごとに角度0固定が必要で、全0化の回転コストは高いこと。評価はスイッチ数だけでなく、初期角度からの回転下限、使用レーン混雑、既存domain減少量を含める。次の試作は、角度0レーン分解の前計算、回転別switch表、通常ペアの最小スイッチ探索の順で行う。

経路Beam内部も具体角度ではなく回転domainを伝播するようにした。各ノードは `(cell,enter,out)` を満たす回転maskを保持し、同一タイルの別線分を再訪したときは以前のmaskとの積集合を取る。積集合が空なら不可能として棄却し、残る場合は初期角度からの最小回転コストの増分だけを評価する。これにより、最初の通過で2回転が可能な接続を1回転へ早期固定せず、後の通過方向に合わせて選べる。tester上の不正解は0件。seed 1〜10の2回評価は `mean_log10=5.153217/5.149242` で、完成経路適用時だけdomainを残す従来版 `5.142420/5.142833` に対し、2回平均差 `+0.008603`、生スコア幾何平均で約+2.0%だったため採用する。通常経路は同一タイル再訪を禁止しているので、主な効果は英雄経路Beamの合法な別線分再訪の自由度増加である。

複数の特殊経路を逐次構築する間にもdomainを保持し、同じタイルの別線分を共有させる実験を行った。domainを通常経路の再構築まで残す版は2回評価で `mean_log10=5.151359/5.146972`。特殊経路を置き終えた直後に具体化し、特殊経路間だけ共有する限定版も `5.147101/5.145508` で、基準 `5.153217/5.149242` に対する2回平均差は `-0.004925`、生スコア幾何平均で約-1.13%だった。共有可能な先行経路のタイルへ後続Beamが寄りすぎ、複数経路の線分配分が悪化する。特殊経路間の単純domain共有は不採用とし、経路Beam内の同一経路再訪だけにdomainを使う。

長い未接続経路を局所回廊内で短い誤接続へ張り替えるBeamと、周囲を部分破壊して供与経路を復元する再構築を18秒条件で試した。短い仮想matching自体は作れるが、壊したmatched経路を同じ領域内で復元できず、真スコア改善は0件だった。局所回廊では必要な所有線分交換の連鎖を収められないため、この専用近傍は削除した。

通常SAの前に400msの経路圧縮SAを置いた。開始盤面で代表経路を固定して選び、代表経路の加重得点を保護しつつ、その他のmatched経路の生の長さ、unmatched terminal path長、回転数を罰する。圧縮評価だけが良くても接続数を落とした盤面は後段へ渡さず、開始時以上の `k` を保った状態のうち真スコア最大だけを採用する。代表上限3本のseed 1〜10・2回平均は `mean_log10=5.155711`。代表上限1本は `5.156475/5.160837`、2回平均 `5.158656` で3本版より `+0.002945`、生スコア幾何平均で約+0.68%だった。準主役まで保護するより、主役1本以外を圧縮して後段SAへ線分を渡す方が強いため、代表上限1本を採用する。

距離の近い未接続ペアを追加する `connect_repair` は、本来ペアの六角距離を候補順の第1キーにした。完成候補では変更前にmatchedだった各指定ペアを全件再追跡し、1組でも別出口へ流れたら棄却する。探索途中の一時切断は許すが、完成時には元の接続をすべて復元するハード制約である。各matched経路のtransitionを常時固定するdomain版は試行しても完成0件で、必要な一時切断まで禁止するため不採用。18秒・領域25・Beam 128のオラクルではseed 5/8で3タイル変更による `Δk=+1` の正改善が見つかった。提出用は領域25・Beam 96・1回80msへ高速化し、seed 1〜10の2回評価は `mean_log10=5.159919/5.159745`、従来2回平均5.158656との差は `+0.001176`、生スコア幾何平均で約+0.27%だったため採用する。
## 実装リファクタリングで有効だった高速化
時間制探索では、盤面隣接・接続方向・回転コスト・domain 最小回転を前計算し、評価用 stamp 配列と出口 partner 表を反復間で再利用すると、探索内容を変えずに反復数を増やせる。差分評価は affected mask の立っている pair だけを trailing_zeros で列挙する。Beam は小さな候補配列を固定長配列に置き、親の訪問 bitset を最後の子へ move するとヒープ確保を減らせる。Tester の入力 pipe は EOF が即時に来るとは限らないため、対話型 tester で read_to_end 型 Scanner は使用しない。
## SA温度の再調整
seed 1〜10で compact SA の `1e6→20`, `2e6→20`, `3e6→100` と、rotation SA の開始温度300・終了温度0.1・matched数による線形/平方根スケールを比較した。compact は `2e6→100`、rotation は固定 `240→0.01` が最も安定し、低温化はcompact後の盤面多様性を失い、高温化と盤面規模補正は特に大型seedで既存接続を壊す時間を増やした。compactの近傍はランダムな1〜2セル（小盤面1〜3セル）の回転だけで、未接続pair専用接続は後段rotation SAの `connect_repair` にのみ存在する。

## compact SAへの大近傍追加実験
compact段へ `connect_repair`、`local_extend`、`restore_repair` を各1回混ぜる実験を行った。`connect_repair` は一部seedで `k` を1〜2増やせたが、重いBeamがcompact反復を削り、後段へ渡す盤面も変わって `mean_log10=5.153095`（基準5.160033）まで低下した。軽量回廊版、終盤の `local_extend`、`restore_repair`、全探索後の追加restoreもそれぞれ悪化した。さらに隣接2セル回転を25%および乱数消費を揃えた1/16頻度で試したが、後者も5.153633だった。compact段は主役以外を広く圧縮する役割に専念させ、接続・復元の大近傍は盤面評価が整った後段rotation SAでのみ使う。

## 未使用経路短縮と1経路交換による接続追加
wrong-terminal間の未使用経路を、matched経路の回転を固定した通常route Beamで同じ出口間へ短く張り直す `path_shortcut` を試した。経路候補は生成できたが、盤面全体のunmatched長と真スコアを同時に改善する候補はseed 1〜10で0件であり、compact置換時は `mean_log10=5.155258`（基準5.160033）だった。

未接続pairを一度接続し、切れた既存matched経路が最大1本なら、そのvictimを固定済み新経路の周囲で再接続する `augment_exchange_1` も試した。被害数を制限しない最短routeは全候補で2本以上を破壊した。Beam中の被害maskを実適用回転基準で1本以下に制限するとseed 4で既存接続を壊さない `k+1` が1件見つかったが、compact前に入れると探索地形が変わり、ハイブリッド最終値は5.150043まで低下した。compact後25msでは採用0件、rotation SA後80msではvictim復元まで完成した候補が1件あったが真スコア低下のため棄却され、平均5.155262だった。現盤面は経路密度が高く、短い新経路でも複数matched経路を横切る一方、既存接続を全固定したvictim復元は自由度が不足する。1本交換を再検討するなら、victimだけでなく新経路も同時に再配線する二経路同時domain Beamが必要である。今回の逐次版は不採用とした。

二経路同時domain Beamも実装して検証した。まず対象2経路以外のmatched経路を固定し、新規pairとvictimを両順序で構築しながら共有タイルのdomainを交差させた。rotation SA後のseed 1〜10では最大72組を試し、二本とも完成2件、安全2件、真スコア改善0件だった。第一経路の候補を同一最短層の上位6本へ広げても完成数は増えなかった。さらに2本のfrontierを1タイルずつ交互展開し、各Beam nodeが共有domainを保持する専用同時Beamへ変更したが、完成1件、改善0件だった。compact直後へ移し、rotation時間を800ms延長して探索量を維持した実験でも完成0件だった。探索自体は0〜9msで空になっており、時間・Beam幅不足ではなく、victim 1本だけを解放した領域には新規pairを通す実行可能解がほぼない。二経路交換は不採用とし、次に行うなら新規pair＋victim 2本の三経路交換、または複数接続を一度外してまとめて再構築するLNSが必要である。

compact SAを完全に無効化し、その400msをrotation SAへ渡す比較をseed 1〜10で各2回行った。同時期の `mean_log10` 2回平均は有効版5.155000、無効版5.155945で、無効版が+0.000945（生スコア幾何平均で約+0.22%）だった。rotation SA反復数の10 seed合計は有効版約4,530万、無効版約4,830万で約6.6%増えた。差は時間制探索の振れに対して小さいが、少なくとも現構成ではcompact SAの明確な正効果は確認できず、無効化してもスコアは落ちなかった。コードは追加判断まで有効状態を維持する。

compact SAを400msから1,000msへ延長し、総時間を固定してseed 1〜10を2回比較した。1,000ms版の `mean_log10` は5.154438/5.156107、平均5.155272で、400ms版2回平均5.155000と実質同等だった。compact反復は10 seed合計約67万から約168万へ増えた一方、rotation SA反復は約4,530万から約4,250万へ減り、時間交換による明確な改善はなかったため400msを維持する。

初期構築の最初のlayered外周接続時間を450/700/1,000msで比較した。seed 1〜10の2回平均 `mean_log10` は順に5.152805/5.155000/5.154345だった。各設定とも生成した構築候補は10 seed合計110件で変わらず、短縮しても後続候補は増えず、延長しても追加候補は得られなかった。外周品質と後続時間の釣り合いが最も良い700msを維持する。時間値は `LAYERED_OUTER_LIMIT_MS` と `LAYERED_SPECIAL_LIMIT_MS` として先頭へ移した。
## Multi-tile relational choice for same-boundary-tile pairs (2026-08-15)

An exit pair whose two ports belong to the same boundary tile can be connected in one tile, but a solution may instead spend a long surrounding route on that pair.  Detect these pairs geometrically from `board.exits`; orientation zero is not a valid proxy.  For every triangle touching the direct tile, enumerate all 216 joint rotations as one tuple.  Keep only tuples where the target pair has length 1, another pair's contribution increases, matched count does not decrease, and the exact score increases.  Resolve disjoint tuples atomically after the ordinary signature postprocess.

Periodic tuple toggles inside rotation SA changed the time-based trajectory and reduced the score, so they were rejected.  A separate 500 ms SA with the direct tile locked made about 80k--1.8M iterations per seed but produced no improvement and was also removed.  The final exact tuple resolver is monotone within each run: in the final seed 1--10 evaluation it accepted four tuples for +6102 on one seed and two tuples for +1144 on another while preserving `k`; `mean_log10=5.1583165269`.  On seed 1, pair 18--19 was correctly detected at length 23, but forcing it direct made the branch too destructive and no safe three-tile transfer existed, so the non-decreasing resolver retained the original.
## Outer-priority experiment for boundary tiles (2026-08-15)

Tested precommitting same-tile target pairs before the ordinary outer-route order: (1) exactly two exits, fixed to the cheapest direct orientation; (2) exactly three exits, retaining the orientation domain that connects the paired exits.  The three-exit case was then split geometrically: consecutive perimeter exit IDs were left to the ordinary route search so both direct and around-the-perimeter routes remained candidates, while pairs with one exit between them received the local connection domain.

Applying both rules to every construction over-constrained the other two strands of those tiles.  `mean_log10` was 5.156281 for the first combined version and 5.153967 after the three-exit classification, versus 5.158317 immediately before the experiment.  Rule 1 alone was noisy (5.160245 and 5.157577 in two runs), but was not robust in a third run.  Keeping the original layered construction and using the new priority only in legacy branches gave 5.155903 and 5.153504.  Since the priority branches were rarely selected and the two-run mean still fell, the whole change was rejected.  If revisited, the local direct/domain state should be carried as an explicit alternative branch without changing the wall-clock trajectory of the baseline construction.

Follow-up: rule 1 (exactly two exits forming a pair) was restored without any three-exit rule.  Applying it only to layered construction scored 5.157278, only to legacy scored 5.155749, and splitting existing trials between fixed and flexible policies scored 5.157609.  Applying it to both constructors scored 5.158644 initially and 5.156616 on a repeat.  Four-thread wall-clock runs are noisy because construction deadlines alter the selected start and timed repair schedule; the broader set of earlier direct-only runs favors retaining the rule in both constructors.  The two branch switches remain top-level constants so this choice can be retested without touching construction logic.

The next layered-only experiment disabled legacy construction completely and reserved prefixes of the special-pair order, producing candidates with one, two, and three protected special routes.  A multi-route candidate divides its special-search time among the remaining routes, recomputes damage after every accepted route, and carries compatible orientation domains across routes.  With the layered special allowance raised from 350 ms to 600 ms, the two seed 1--10 runs scored `mean_log10=5.162185/5.161411` (mean 5.161798), above the earlier layered-priority/legacy-enabled 5.157278.  Three special routes were actually completed on several instances.  With legacy disabled, the two measured rotation-SA totals were 50,076,160 and 48,902,912 iterations over ten seeds, so the extra layered candidates still left substantial downstream search.  Keep legacy behind a top-level disabled switch while evaluating this construction.

## Protected multi-trunk ruin/recreate A/B (2026-08-15)

The remaining gap is allocation rather than empty capacity, so a 150 ms structural branch now competes with a 250 ms compact branch inside the former 400 ms compact allowance.  The structural branch selects the two highest-bonus matched trunks and four nearby low-value matched paths.  It removes their constraints together, while every unselected matched path contributes local `(enter,out)` constraints to a shared orientation domain.  This protection was essential: unlocking whole tiles broke 12--42 unrelated matches per trial, whereas the protected version can preserve all non-ruined paths.  The two trunks are rebuilt against the shared domain, the four victims are restored in shortest-distance order, domains are resolved, and only an exact-score improvement is retained.  The structural and compact candidates are generated independently from the same board, then the better exact result is passed to rotation SA.  This is a practical shared-domain group reconstruction, not yet a fully joint multi-frontier beam.

On target seeds 54, 62, 70, 82, 11, and 26, two-run `mean_log10` was 6.103363 versus 6.102254 for compact-only (`+0.001109`).  On seed 1--10, two-run mean was 5.163490 versus 5.161798 (`+0.001692`).  The LNS branch was selected on four and six of the nine eligible cases in the two ten-seed runs; the smallest case stayed on compact.  Rotation SA retained about 47.9--49.7 million total iterations over ten seeds.  Keep the exact A/B gate: running compact serially on the LNS result or spending the full 400 ms on LNS was less robust, especially on seed 62.

## Layered outer board beam (2026-08-15)

The three-layer outer constructor now keeps a beam of complete partial allocations instead of committing the first route for every pair.  Each state stores the orientation representatives, per-tile orientation domains, connected-pair set, layer counts, connected count, optimistic reachable count, committed route length, and domain rotation lower bound.  For every pair it retains the skip transition and up to four distinct shortest route alternatives from the route beam.  A route that makes any previously reachable unresolved pair unreachable is rejected.  States are ranked with connected count dominant, then optimistic reachability, route length, and rotation lower bound.  Beam width 8 was enough; width 16 spent more work on similar states and scored slightly lower.

On seed 1--10, width 8 scored `mean_log10=5.168166/5.166812`, mean 5.167489.  The preceding protected-LNS version scored 5.163490 over two runs, so the outer board beam added `+0.003999 log` while preserving roughly 49 million rotation-SA iterations.  Width 16 scored 5.167784/5.166581, mean 5.167182, and was rejected.  The construction frequently increased later-layer connection counts and produced new bests on seeds 5, 7, 9, and 10 during the comparison.  Candidate scarcity is not yet part of pair ordering; the current experiment isolates the benefit of keeping several whole-board allocation states and several routes per pair.

## Shallow-detour route alternatives (2026-08-15)

For endpoint cells at hex distance at most three, the route beam was extended two depths beyond the first goal so that lengths `d`, `d+1`, and `d+2` could coexist.  Committing those alternatives inside the outer board beam reduced seed 1--10 `mean_log10` to 5.161753 at width 8, 5.162099 at width 16, and 5.160578 at width 32.  The loss therefore was not beam-width scarcity.  Reserving four of sixteen states for detour-bearing boards also remained below the shortest-only beam.

Passing the best shortest and detour boards through the special-route/domain stage was tested next.  Re-running the special search per board consumed too much downstream time (`mean_log10=5.155474`).  Replaying the shortest board's special route onto the detour board was invalid: intersecting tile domains independently loses the cross-tile route correlation and produced an invalid orientation in one seed.  Both variants were rejected.  The safe default keeps up to four distinct shortest paths in a width-8 board beam (`mean_log10=5.167552` in the restore run).  A future detour implementation must retain each whole route alternative as one correlated choice and resolve that choice jointly with later routes; it must not collapse alternatives into independent per-tile domains.

The correlated-choice version was subsequently implemented safely.  A deferred pair retained whole `Route` objects, checked joint domain feasibility after every later commitment, reserved separate beam slots, and materialized all retained board states with exact evaluation.  Two deferred choices scored 5.163553 initially and 5.165682 after protecting the ordinary eight states.  Restricting it to one deferred choice and one extra state scored 5.167836/5.164759 (mean 5.166297); actual detours were selected on a few cases, but the improvement was not stable.  The final shortest-only width-8 run scored 5.169155.  Keep both short-detour generation and deferred materialization disabled: tile domains remain useful inside each shortest route, while multiple complete shortest routes and multiple board states provide the robust freedom.

## Second construction beam with virtual wrong connections (2026-08-15)

After the first layered construction, take up to three unmatched pairs and six matched paths with the largest excess length over endpoint distance.  Assign unmatched pairs one-to-one to long paths by minimizing the virtual wrong-connection cost `min(d(u0,l0)+d(u1,l1), d(u0,l1)+d(u1,l0))`.  The wrong links are only a pairing/ruin-selection model and never remain in the scored board.  Preserve every unselected matched strand as an `(enter,out)` orientation domain, release each selected unmatched/long pair group, then run a second width-8 board beam on their original partners, with up to four distinct shortest routes per step.

Using the bonus-oriented special route search here could consume 0.6--2.5 seconds per instance.  The ordinary shortest-route search reduced the second pass to 0--113 ms and produced repeatable construction gains such as `k=31->33` and `k=72->75`.  Same-`k` exact-score replacements changed the downstream timed SA trajectory without serving the connection objective, so final acceptance requires both a true-score improvement and `k` increase.  The final seed 1--10 run scored `mean_log10=5.168205` versus the preceding 5.169155; a prior run scored 5.169103, and even seed 1 where the new pass did no work varied from 520 to 506 to 429.  Treat this difference as time-based variance; retain the bounded pass because it adds connections structurally without a material iteration loss.

Compact-SA removal was tested after adding the second construction beam.  Keeping the small beam and giving the saved time to rotation SA scored `mean_log10=5.153179`; expanding the second beam from width 8/three unmatched/six long candidates to width 16/five/ten recovered to 5.166752.  The expanded beam plus compact SA scored 5.166078, while compact SA plus the small beam scored 5.168205.  Thus the construction beam can replace much of compact SA's effect, but not all of it, and running both large searches competes for time.  Keep compact SA enabled and the second beam bounded at width 8 with at most three virtual exchanges.  A top-level compact switch remains for reproducible A/B tests.

## Pair-order beam experiment (2026-08-15)

Allowing every board state to choose among three next pairs, ranked by narrow endpoint domains, short distance, and ordinary priority, reduced seed 1--10 `mean_log10` to 5.152055.  Restricting order branching to the first three decisions of each layer and giving route scarcity a stronger rank still scored only 5.151258.  The outer 700 ms budget then processed fewer late pairs, but the deeper problem was beam comparability: an easy pair connected early dominates the intermediate `connected_count`, so width 8 discards both constrained-order branches and sometimes the original fixed-order branch before a full layer can be compared.  Disable pair-order branching.  A future retry must retain the complete fixed-order width-8 beam in a separate reserved pool and add order variants outside it; sharing the same eight slots is not safe.

## Full-board construction and segment-first ranking (2026-08-15)

The width-8 construction beam was expanded from boundary depth 0--2 to every valid tile.  Alternatives remain distinct shortest paths only, with skip plus at most four routes per pair.  Reserved special pairs are still excluded and routed afterward.  Two full-board runs scored `mean_log10=5.167202/5.167519`, mean 5.167360, versus 5.168205/5.167042 (mean 5.167624) for the two nearby outer-only runs.  This was effectively flat, while large-instance construction connectivity increased, including 117 matches where the outer version had 113.

Beam ranking was then changed from `connected, optimistic reachability, used segments, rotations` to the lexicographic order `connected, fewer used segments, optimistic reachability, fewer rotations`.  Connected count must remain the hard first key because otherwise the zero-segment all-skip state wins; among equal-`k` states, segment use is now the primary evaluation.  This full-board segment-first version scored `mean_log10=5.168411`, above both nearby means, and is retained.

The second tie-break was refined to the number of tiles whose representative orientation differs from the initial board, producing `connected, fewer used segments, fewer actually rotated tiles, optimistic reachability, rotation-domain lower bound`.  Counting only domains that exclude the initial orientation was misleading: a domain that still contains the initial orientation can nevertheless resolve to a rotated representative, and that version fell to `mean_log10=5.165236`.  Counting the actual representative orientations scored `mean_log10=5.168662`, slightly above segment-first alone, so the actual rotated-tile count is retained as the second cost criterion after used segments.

## Apply/revert tree board beam (2026-08-15)

The full-board beam was converted from cloning complete orientation/domain/connected vectors for every child to rhoo-style tree traversal.  One mutable board is moved between selected candidates by reverting route deltas to their lowest common ancestor and applying deltas down to the target.  A node stores only its parent, depth, pair ID, and the old/new orientation and domain of route tiles.  Connected count and used segments remain candidate accumulators; rotated-tile count and domain rotation lower bound are updated per changed tile.  Optimistic reachability still rebuilds its union-find and is now the dominant non-route cost.

Across the same 30 construction-beam calls on seeds 1--10, the cloned beam generated 15,777 accepted children and made 30,912 reachability checks in 9,989 ms.  The tree beam generated 15,791 children and made 30,955 checks in 9,862 ms, a 1.3% reduction without reducing search volume.  Its measured `mean_log10` was 5.169109 versus 5.167090 for the adjacent cloned run, though wall-clock trajectory noise remains.  Retain the tree beam behind a top-level switch.  The next speed target is optimistic reachability, not state storage.

Increasing the tree-beam width from 8 to 10 scored `mean_log10=5.168415/5.168474` (mean 5.168445) and raised accepted child expansions from about 15.8k to 19.4k over 30 beam calls.  Width 12 scored 5.166180 and width 16 scored 5.163011; their aggregate beam time rose from 9.86 seconds at width 8 to 11.82 and 12.98 seconds, so later pairs increasingly hit the per-construction deadline.  Width 10 is retained as the largest stable increase; wider beams need delayed or cheaper optimistic-reachability checks before they can usefully add diversity.

The per-beam 700 ms cutoff was then removed: a beam that has started processes the complete pair order, while the construction loop still declines to start another candidate after its 4.5 second budget.  Complete width 8 scored 5.165309, width 10 scored 5.167426, and width 12 scored 5.166149.  Width 10 was the best construction/SA balance; reducing the width gave more SA iterations but lost too much initial routing quality, while width 12 spent more time without improving it.

Late constrained pairs were indeed wasting work in long one-directional searches.  For every ordinary route query, reverse BFS over `(cell, enter-side)` states now computes relaxed reachability under the current orientation domains.  States with no route to the target are pruned, but the reverse distance is deliberately not used in route ranking: ranking by it shortened beam time but collapsed useful allocation detours and scored only 5.153234.  Unreachable-only pruning with the original hex-distance ranking scored `mean_log10=5.169324/5.169649` (mean 5.169487), retained 44.96--46.73 million rotation-SA iterations, and finished within 9.42 seconds.  Keep width 10 and complete pair-order processing with reverse BFS used only as a feasibility filter.

Reverse BFS storage is reused across route queries.  A stamp array and wrapping epoch replace filling the distance array with infinity; the queue is a retained `Vec` with a head index rather than a newly allocated `VecDeque`.  Only states stamped in the current epoch have valid distances.  This reduced aggregate tree-beam time from about 20.37 to 20.02 seconds over 30 seed 1--10 construction calls and aggregate construction time from about 23.66 to 23.32 seconds.  The run scored `mean_log10=5.169693` with 47.18 million rotation-SA iterations.  The same scratch is also reused by the second construction beam.

Complete-beam widths below ten were swept as well.  Seed 1--10 `mean_log10` was 5.167112 at width 1, 5.168120 at width 2, 5.167505 at width 4, 5.167170 at width 6, and 5.165309 at width 8, versus 5.169693 at width 10.  Width 2 was the narrow-side local optimum: aggregate tree-beam time fell from 20.02 to 4.00 seconds and rotation SA rose from 47.18 to 49.38 million iterations, but the extra SA did not recover the lost routing diversity.  Keep width 10; width 2 may be useful only as a separate cheap candidate rather than a replacement.
